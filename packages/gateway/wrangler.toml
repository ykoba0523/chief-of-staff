name = "chief-of-staff-gateway"
main = "src/index.ts"
compatibility_date = "2025-03-10"

# [vars]
# API_ENDPOINT = ""  # Mastra Cloud のエンドポイント
# MASTRA_SHARED_SECRET = ""  # 共有シークレット

# Secrets (wrangler secret put で設定):
# - SLACK_SIGNING_SECRET
# - MASTRA_API_URL
# - MASTRA_SHARED_SECRET

# 定時Push用 Cron Trigger (Phase 6 で有効化)
# [triggers]
# crons = ["0 0 * * 1-5", "30 3 * * 1-5", "0 9 * * 1-5"]
# ↑ UTC: 0:00=JST 9:00, 3:30=JST 12:30, 9:00=JST 18:00
